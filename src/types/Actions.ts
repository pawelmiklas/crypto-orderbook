export enum Action {
  push = "push",
  subscribePublicError = "subscribe-public-error",
  subscribePrivateError = "subscribe-private-error",
  subscribePrivateConfirm = "subscribe-private-confirm",
  subscribePublicConfirm = "subscribe-public-confirm",
  proxyResponse = "proxy-response",
  pong = "pong",
  jsonError = "json-error",
}
